{% extends 'admin/admin_base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            
            <h2>Add User</h2>
            <div class="well">
                {{ form_start(addUserForm) }}
                {{ form_widget(addUserForm) }}
                <input type="submit" value="Submit" class="btn btn-lg">
                {{ form_rest(addUserForm) }}
            {{ form_end(addUserForm) }}
            </div>
        </div>
    </div>
{% endblock %}
