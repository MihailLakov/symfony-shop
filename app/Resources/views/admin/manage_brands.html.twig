{% extends 'admin/admin_base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h2>Brands Catalog</h2>
            </div>
            <div class="col-xs-12">
                <a href="{{path('admin-brand-create')}}" class="btn btn-primary"> <span class="glyphicon glyphicon-plus"></span>Add Brand</a>            
            <table class="table">
                <tr>
                    <th>ID</th>
                    <th>Title</th>
              
                </tr>
                {% for brand in brands %}
                <tr>
                    <td>{{category.id}}</td>
                    <td>{{category.title}}</td>
                                     
                    <td>  <a href="{{path('admin-brand-edit',{id:brand.id}) }}"  class="btn btn-info">Edit</a></td>          
                    <td>  <a href="{{path('admin-brand-delete',{id:brand.id}) }}"  class="btn btn-danger">Delete</a></td>    
                </tr>
                {% endfor %}
            </table>
            </div>
           
        </div>
    </div>
{% endblock %}
